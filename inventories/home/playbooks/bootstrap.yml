---
- name: Bootstrap hosts
  hosts: all
  become: true

  roles:
    - mps.base.identity
    - mps.os.system_settings
    - mps.os.package_manager
    - mps.os.system_tools
    - mps.users.users
    - mps.users.groups
    - mps.users.ssh
    - mps.users.sudo
