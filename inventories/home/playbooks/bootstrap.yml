---
- name: Bootstrap hosts
  hosts: all
  become: true

  roles:
    - odem.base.identity
    - odem.os.system_settings
    - odem.os.package_manager
    - odem.os.system_tools
    - odem.users.users
    - odem.users.groups
    - odem.users.ssh
    - odem.users.sudo
